# Frontend Rules

- Functional components only, hooks for state — no class components
- API responses arrive in snake_case — always convert to camelCase in the api/ layer before use
- JWT stored in memory only — never localStorage
- Min supported width: 1024px
- Run `npm run lint` before committing
